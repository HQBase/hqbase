name: Staging E2E

on:
  workflow_dispatch:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]

concurrency:
  group: hqbase-pro-staging-e2e
  cancel-in-progress: false

jobs:
  community-upgrade-migration:
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    environment: hqbase-staging
    timeout-minutes: 20
    env:
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.HQBASE_E2E_CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.HQBASE_E2E_CLOUDFLARE_API_TOKEN }}
      DEPLOYMENT_NAME: upgrade-${{ github.run_id }}
      D1_NAME: hqbase-upgrade-${{ github.run_id }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
      - uses: pnpm/action-setup@v4
        with:
          version: 11.7.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Check out Community
        uses: actions/checkout@v4
        with:
          repository: HQBase/hqbase
          ref: 981f12508f53e0accbf840328f8629a8c21990b4
          path: .staging/community
      - name: Install Community operator dependencies
        working-directory: .staging/community
        run: pnpm install --frozen-lockfile
      - name: Create remote Community data resources
        working-directory: .staging/community
        run: >-
          pnpm hqbase install
          --name "$DEPLOYMENT_NAME"
          --d1-name "$D1_NAME"
          --skip-build
          --skip-deploy
      - name: Seed Community retention marker
        working-directory: .staging/community
        run: >-
          pnpm exec wrangler d1 execute "$D1_NAME"
          --remote
          --command
          "INSERT INTO app_settings (key, value_json, created_at, updated_at)
          VALUES ('e2e-upgrade-probe', '{\"retained\":true}', datetime('now'), datetime('now'))"
      - name: Preflight Community to Pro migration
        run: >-
          pnpm hqbase-pro upgrade
          --from-community
          --database "$D1_NAME"
          --remote
          --dry-run
      - name: Apply backed-up Community to Pro migration
        run: >-
          pnpm hqbase-pro upgrade
          --from-community
          --database "$D1_NAME"
          --remote
          --yes
      - name: Verify Community data retention
        run: |
          result=$(pnpm exec wrangler d1 execute "$D1_NAME" --remote --json \
            --command "SELECT value_json FROM app_settings WHERE key = 'e2e-upgrade-probe'")
          jq -e '.[0].results[0].value_json == "{\"retained\":true}"' <<<"$result"
      - name: Destroy Community upgrade resources
        if: always()
        working-directory: .staging/community
        run: pnpm hqbase destroy --name "$DEPLOYMENT_NAME" --scope all --yes

  fresh-install-and-bridge:
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    environment: hqbase-staging
    timeout-minutes: 30
    env:
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.HQBASE_E2E_CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.HQBASE_E2E_CLOUDFLARE_API_TOKEN }}
      HQBASE_PRO_STAGING_ACCESS_CLIENT_ID: ${{ secrets.HQBASE_E2E_ACCESS_CLIENT_ID }}
      HQBASE_PRO_STAGING_ACCESS_CLIENT_SECRET: ${{ secrets.HQBASE_E2E_ACCESS_CLIENT_SECRET }}
      HQBASE_PRO_STAGING_EMAIL_DOMAIN: ${{ secrets.HQBASE_E2E_EMAIL_DOMAIN }}
      HQBASE_PRO_STAGING_OWNER_EMAIL: owner@${{ secrets.HQBASE_E2E_EMAIL_DOMAIN }}
      HQBASE_PRO_STAGING_OWNER_PASSWORD: ${{ secrets.HQBASE_E2E_OWNER_PASSWORD }}
      HQBASE_PRO_STAGING_SENDER: hello@${{ secrets.HQBASE_E2E_EMAIL_DOMAIN }}
      HQBASE_PRO_STAGING_URL: https://${{ secrets.HQBASE_E2E_APP_HOSTNAME }}
      HQBASE_PRO_STAGING_BRIDGE_TOKEN: ${{ secrets.HQBASE_PRO_E2E_BRIDGE_TOKEN }}
      HQBASE_PRO_STAGING_BRIDGE_HOST: ${{ secrets.HQBASE_PRO_E2E_BRIDGE_HOST }}
      HQBASE_BRIDGE_ACCEPTANCE_BIN: /tmp/hqbase-bridge-acceptance
      DEPLOYMENT_NAME: e2e-${{ github.run_id }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
      - uses: pnpm/action-setup@v4
        with:
          version: 11.7.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.x"
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec playwright install --with-deps chromium
      - name: Check out the pinned bridge implementation
        uses: actions/checkout@v4
        with:
          repository: HQBase/hqbase-mail-bridge
          ref: 5416175d622ffcfcfc093f583ce5c26fc25c70e2
          path: .staging/bridge
          ssh-key: ${{ secrets.HQBASE_BRIDGE_REPO_DEPLOY_KEY }}
      - name: Build the headless IMAPS/SMTPS acceptance client
        working-directory: .staging/bridge
        run: go build -o "$HQBASE_BRIDGE_ACCEPTANCE_BIN" ./cmd/acceptance
      - name: Deploy disposable Pro installation
        run: >-
          pnpm hqbase-pro install
          --name "$DEPLOYMENT_NAME"
          --worker-name hqbase-pro-e2e-staging
          --domain "$HQBASE_PRO_STAGING_EMAIL_DOMAIN"
          --no-email
          --app-domain "${HQBASE_PRO_STAGING_URL#https://}"
          --bridge-token "$HQBASE_PRO_STAGING_BRIDGE_TOKEN"
      - name: Wait for the disposable Pro custom domain
        run: |
          for attempt in $(seq 1 150); do
            if curl --fail --silent --show-error \
              -H "CF-Access-Client-Id: $HQBASE_PRO_STAGING_ACCESS_CLIENT_ID" \
              -H "CF-Access-Client-Secret: $HQBASE_PRO_STAGING_ACCESS_CLIENT_SECRET" \
              "$HQBASE_PRO_STAGING_URL/api/health" >/dev/null; then
              exit 0
            fi
            sleep 2
          done
          echo "Disposable Pro custom domain was not healthy within 300 seconds"
          exit 1
      - name: Prepare an N-1 Pro installation
        id: release-versions
        run: |
          current_version=$(node -p "require('./package.json').version")
          previous_version=$(node -p "require('./package.json').hqbaseRelease.minimumVersion")
          test "$current_version" != "$previous_version"
          echo "current=$current_version" >> "$GITHUB_OUTPUT"
          echo "previous=$previous_version" >> "$GITHUB_OUTPUT"
          config=".hqbase-pro/deployments/$DEPLOYMENT_NAME/wrangler.jsonc"
          secrets=".hqbase-pro/deployments/$DEPLOYMENT_NAME/secrets.json"
          node --input-type=module -e '
            import fs from "node:fs";
            const path = process.argv[1];
            const version = process.argv[2];
            const config = JSON.parse(fs.readFileSync(path, "utf8"));
            config.vars.HQBASE_APP_VERSION = version;
            fs.writeFileSync(path, `${JSON.stringify(config, null, 2)}\n`);
          ' "$config" "$previous_version"
          pnpm exec wrangler d1 execute DB --remote --config "$config" \
            --command "UPDATE pro_release_state SET installed_version = '$previous_version', updated_at = datetime('now') WHERE singleton = 1"
          pnpm exec wrangler deploy --config "$config" --secrets-file "$secrets"
      - name: Verify the Fly bridge can reach Pro
        run: |
          for attempt in $(seq 1 150); do
            if curl --fail --silent --show-error \
              "https://$HQBASE_PRO_STAGING_BRIDGE_HOST/ready" >/dev/null; then
              exit 0
            fi
            sleep 2
          done
          echo "Fly bridge could not reach Pro within 300 seconds"
          exit 1
      - name: Exercise Pro app passwords through real IMAPS and SMTPS
        env:
          HQBASE_PRO_STAGING_EXPECT_UPDATE_VERSION: ${{ steps.release-versions.outputs.current }}
        run: pnpm test:e2e:staging
      - name: Apply the guarded Pro N-1 update while the bridge remains running
        run: |
          current_version=$(node -p "require('./package.json').version")
          previous_version="${{ steps.release-versions.outputs.previous }}"
          config=".hqbase-pro/deployments/$DEPLOYMENT_NAME/wrangler.jsonc"
          node --input-type=module -e '
            import fs from "node:fs";
            const path = process.argv[1];
            const version = process.argv[2];
            const config = JSON.parse(fs.readFileSync(path, "utf8"));
            config.vars.HQBASE_APP_VERSION = version;
            fs.writeFileSync(path, `${JSON.stringify(config, null, 2)}\n`);
          ' "$config" "$current_version"
          pnpm build
          HQBASE_TARGET_VERSION="$current_version" CONFIG_FILE="$config" node --input-type=module -e '
            import { runDeployLifecycle } from "./scripts/hqbase-pro/deploy.mjs";
            runDeployLifecycle({ configFile: process.env.CONFIG_FILE });
          '
          release=$(pnpm exec wrangler d1 execute DB --remote --config "$config" --json \
            --command "SELECT installed_version FROM pro_release_state WHERE singleton = 1")
          history=$(pnpm exec wrangler d1 execute DB --remote --config "$config" --json \
            --command "SELECT from_version, to_version, state FROM pro_update_history ORDER BY started_at DESC LIMIT 1")
          jq -e --arg version "$current_version" '.[0].results[0].installed_version == $version' <<<"$release"
          jq -e --arg from "$previous_version" --arg version "$current_version" \
            '.[0].results[0] == {"from_version":$from,"to_version":$version,"state":"verified"}' \
            <<<"$history"
      - name: Verify deep readiness after redeploy
        run: |
          curl --fail --silent --show-error \
            -H "CF-Access-Client-Id: $HQBASE_PRO_STAGING_ACCESS_CLIENT_ID" \
            -H "CF-Access-Client-Secret: $HQBASE_PRO_STAGING_ACCESS_CLIENT_SECRET" \
            -H "Authorization: Bearer $HQBASE_PRO_STAGING_BRIDGE_TOKEN" \
            "$HQBASE_PRO_STAGING_URL/api/pro/mail-bridge/v2/ready" | jq -e '.ready == true'
          curl --fail --silent --show-error "https://$HQBASE_PRO_STAGING_BRIDGE_HOST/ready" >/dev/null
      - name: Exercise the still-running bridge after Pro redeploy
        run: pnpm test:e2e:staging
      - name: Exercise populated remote backup and restore
        run: |
          config=".hqbase-pro/deployments/$DEPLOYMENT_NAME/wrangler.jsonc"
          database="hqbase-pro-$DEPLOYMENT_NAME"
          pnpm exec wrangler d1 execute "$database" --remote --config "$config" \
            --command "INSERT INTO app_settings (key, value_json, created_at, updated_at) VALUES ('track1-restore-probe', '{\"state\":\"before\"}', datetime('now'), datetime('now'))"
          pnpm hqbase-pro backup --name "$DEPLOYMENT_NAME" --output /tmp/track1-backup.json
          pnpm exec wrangler d1 execute "$database" --remote --config "$config" \
            --command "UPDATE app_settings SET value_json = '{\"state\":\"after\"}', updated_at = datetime('now') WHERE key = 'track1-restore-probe'"
          pnpm hqbase-pro restore --name "$DEPLOYMENT_NAME" --backup /tmp/track1-backup.json --yes
          result=$(pnpm exec wrangler d1 execute "$database" --remote --config "$config" --json \
            --command "SELECT value_json FROM app_settings WHERE key = 'track1-restore-probe'")
          jq -e '.[0].results[0].value_json == "{\"state\":\"before\"}"' <<<"$result"
          curl --fail --silent --show-error \
            -H "CF-Access-Client-Id: $HQBASE_PRO_STAGING_ACCESS_CLIENT_ID" \
            -H "CF-Access-Client-Secret: $HQBASE_PRO_STAGING_ACCESS_CLIENT_SECRET" \
            "$HQBASE_PRO_STAGING_URL/api/health" >/dev/null
          curl --fail --silent --show-error "https://$HQBASE_PRO_STAGING_BRIDGE_HOST/ready" >/dev/null
      - name: Exercise the still-running bridge after restore
        run: pnpm test:e2e:staging
      - name: Verify the successful run is a healthy persistent staging anchor
        run: |
          config=".hqbase-pro/deployments/$DEPLOYMENT_NAME/wrangler.jsonc"
          pnpm exec wrangler d1 info "hqbase-pro-$DEPLOYMENT_NAME" --config "$config" >/dev/null
          pnpm exec wrangler r2 bucket info "hqbase-pro-$DEPLOYMENT_NAME-mail" >/dev/null
          pnpm exec wrangler queues info "hqbase-pro-$DEPLOYMENT_NAME-jobs" >/dev/null
          curl --fail --silent --show-error \
            -H "CF-Access-Client-Id: $HQBASE_PRO_STAGING_ACCESS_CLIENT_ID" \
            -H "CF-Access-Client-Secret: $HQBASE_PRO_STAGING_ACCESS_CLIENT_SECRET" \
            "$HQBASE_PRO_STAGING_URL/api/health" >/dev/null
          curl --fail --silent --show-error "https://$HQBASE_PRO_STAGING_BRIDGE_HOST/ready" >/dev/null
      - name: Retain the non-secret staging manifest for verified cleanup
        uses: actions/upload-artifact@v4
        with:
          name: hqbase-pro-staging-manifest-${{ github.run_id }}
          path: .hqbase-pro/deployments/e2e-${{ github.run_id }}/manifest.json
          if-no-files-found: error
          retention-days: 30
