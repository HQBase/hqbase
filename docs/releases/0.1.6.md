# HQBase Pro 0.1.6

- Snapshots Community attachment references before Pro migrations rebuild the messages table.
- Restores attachment rows idempotently before isolated candidate verification.
- Retains the snapshot for operator recovery until promoted-service verification succeeds.
- Removes the temporary snapshot only after the in-place upgrade is complete.
- Preserves the same R2 attachment objects and existing message relationships.
