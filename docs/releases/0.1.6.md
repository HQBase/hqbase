# HQBase Community 0.1.6

- Binds each in-place Pro candidate to its verified release version and Worker code digest.
- Fails closed before database migration if the stable Pro release changes after candidate upload.
- Requires isolated preview verification to report the exact expected Pro edition and version.
- Preserves the existing Worker, D1 database, R2 bucket, routes, domains, and secret bindings.
- Supports direct updates from Community 0.1.3, 0.1.4, and 0.1.5.
