# HQBase Community 0.1.8

- Validates the immutable version-specific Pro preview instead of relying on a mutable alias.
- Retries only transient preview-route and edge failures while Preview URLs propagate.
- Fails closed immediately when the candidate returns an application validation error.
- Restores the Worker's original Preview URL and workers.dev settings after every outcome.
- Supports direct updates from Community 0.1.3 through 0.1.7.
