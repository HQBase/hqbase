# HQBase Community 0.1.19

- Fixes in-place Pro candidate validation against Cloudflare's current paginated Worker versions
  response.
- Fails safely when Cloudflare returns a malformed versions response instead of exposing a raw
  runtime error in the upgrade UI.
