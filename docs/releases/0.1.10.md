# HQBase Community 0.1.10

- Routes isolated candidate checks through Cloudflare's public Worker-to-Worker fetch path.
- Adds the required `global_fetch_strictly_public` compatibility flag to fresh and signed updates.
- Preserves all customer compatibility flags while adding the required candidate-validation flag.
- Retains pre-upload preview enablement, transient retries, and exact settings restoration.
- Supports direct updates from Community 0.1.3 through 0.1.9.
