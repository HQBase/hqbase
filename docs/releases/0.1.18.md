# HQBase Community 0.1.18

- Fixes in-place Pro candidate uploads against Cloudflare's current strict binding-inheritance API.
- Verifies that the latest uploaded Worker version is still the recorded active signed Community
  version before inheriting existing bindings and secrets.
- Reserves Cloudflare reauthorization recovery for expired grants; provider configuration failures
  now remain retryable without asking the owner to authorize again unnecessarily.
